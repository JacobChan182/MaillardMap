# iOS (MVVM)

This folder is owned by the `ios` agent.

## MVVM layout (skeleton)

```
ios/
  BigBack/
    App/
    Models/
    Views/
    ViewModels/
    Services/
    Resources/
```

## Next steps

- Create an Xcode project in `ios/BigBack/BigBack.xcodeproj`
- Keep UI in `Views/` (SwiftUI) and state/business logic in `ViewModels/`
- Keep networking in `Services/` (API client)
